# Override to point at a remote backend; leave empty to use the Vite dev proxy.
# If using an explicit backend URL, include the scheme: http://localhost:3000
VITE_API_BASE_URL="https://localhost:3000"
